//Determine who goes first with popup: variable turn = human/computer

//Clear the board
var win = false;
var tie = false;
var turn = 'human';

while (win === false && tie === false) {
  console.log('asdf')
  if (turn === 'human'){
    //wait for input
    //update board
    //switch turn
    //if three in a row, win
    break;
  } else if (turn === 'computer') {
    //play some AI, white boy
    //update board
    //switch turn
    //if three in a row, win
  }
  break;
}
